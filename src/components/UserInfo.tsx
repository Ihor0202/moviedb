import React from 'react';

// UserInfo (just hardcoded small circle and name)
const UserInfo = () => {
    return (
        <div>
            UserInfo (just hardcoded small circle and name)

        </div>
    );
};

export default UserInfo;